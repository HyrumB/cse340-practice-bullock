<div class="game-grid">
    <% games.forEach((game) => { %>
        <div class="game-card">
            <img src="<%= game.image_path %>" alt="<%= game.game_name %>">
            <a href="/category/edit/<%= game.game_id %>">Edit</a>
            <a href="/category/delete/<%= game.game_id %>">delete</a>
            <h3><%= game.game_name %></h3>
            <p><%= game.game_description %></p>
        </div>
    <% }); %>
</div>